<?php
$server = "localhost";
$username= "root";
$password= "";
$dbname= "Aiub"

$conn = mysqli_connect($server, $username, $password, $dbname);
if(isset($_POST['insert'])){
    if (!empty($_Post['fname'])) &&
}
